/**
 * The Balance interface.
 */
export interface Balance {
  /** The Balance's ID. */
  idBalance: number;
  /** The User's ID. */
  idUser: number;
  /** The amount in account. */
  balance: number;
}
