/**
 * The User interface.
 */
export interface User {
  /** The User's ID. */
  id: number;
  /** The User's name. */
  name: string;
  /** The User's email. */
  email: string;
  /** The User's birthday. */
  birth: string;
}
