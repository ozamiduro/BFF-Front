import { Transference } from '../../../../Domain/Entity/Transference';

/**
 * The props type for {@link Title}.
 */
interface TransferenceLineProps {
  /** An array of {@link Transference} objects. */
  data: Transference[];
}

export type { TransferenceLineProps };
