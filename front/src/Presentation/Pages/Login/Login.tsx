import { LoginTemplate } from '../../Templates/Login';

/**
 * Renders the Login page.
 *
 * Is the Login's page. It contains the Login's Template.
 *
 * **Usage example**
 *
 * **Default properties**
 *  ```tsx
 * <Login />
 * ```
 * ---
 * @category Page
 */
const Login = () => {
  return <LoginTemplate />;
};

export { Login };
