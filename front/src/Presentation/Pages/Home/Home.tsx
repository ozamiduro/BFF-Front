import { HomeTemplate } from '../../Templates/Home';

/**
 * Renders the Home page.
 *
 * Is the Home's page. It contains the Home's Template.
 *
 * **Usage example**
 *
 * **Default properties**
 *  ```tsx
 * <Home />
 * ```
 * ---
 * @category Page
 */
const Home = () => {
  return <HomeTemplate />;
};

export { Home };
